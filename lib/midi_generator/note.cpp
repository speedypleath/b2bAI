#include "note.h"

// midi_generator::Note::Note(int p, int v, double s, double e)
//     : pitch(p), velocity(v), start(s), end(e)
// {
// }

// std::ostream& operator<<(std::ostream& os, const midi_generator::Note& note) {
//     os << "Note(" << note.pitch << ", " << note.velocity << ", " << note.start << ", " << note.end << ")\n";
//     return os;
// }

// bool midi_generator::Note::operator==(const midi_generator::Note &rhs) const {
//         return pitch == rhs.pitch &&
//             velocity == rhs.velocity &&
//             start == rhs.start &&
//             end == rhs.end;
//     }
    
// bool midi_generator::Note::operator!=(const midi_generator::Note &rhs) const {
//         return !(rhs == *this);
//     }
